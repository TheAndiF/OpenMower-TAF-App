BS_Fix sensor live data subscription
Date: 2026-06-18
Summary:
- Subscribe to sensors/<sensor_id>/data so the sensor page receives the current MQTT live-value structure.
- Parse JSON, BSON, and plain-text payloads from sensor live-data topics.
- Create sensor display states from sensors/settings/json metadata even before legacy sensor_infos data is received.
- Keep sensor metadata editing separated from live values; live values remain read-only.
Changed files:
- lib/io/mqtt_connection.dart
- lib/controllers/sensors_controller.dart
- commit_message.txt
